import React from 'react';
import { Box, Typography } from '@mui/material';
// Hier später Gantt-Bibliothek einfügen, z. B. react-google-charts

const GanttTab = ({ process, activities }) => {
  return (
    <Box>
      <Typography>Gantt-Diagramm wird hier implementiert</Typography>
      {/* Platzhalter für Gantt */}
    </Box>
  );
};

export default GanttTab;